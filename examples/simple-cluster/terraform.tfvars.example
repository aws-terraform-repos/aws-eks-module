# Example variables for simple-cluster
# Copy this file to terraform.tfvars and customize the values

# Required: Your cluster name
cluster_name = "my-test-cluster"

# Optional: Customize these values as needed
cluster_version = "1.32"

# Node group configuration
node_group_instance_types = ["t3.medium"]
node_group_desired_size   = 2
node_group_max_size       = 4
node_group_min_size       = 1

# Security configuration (restrict API access to your IP)
public_access_cidrs = ["0.0.0.0/0"]  # Open to all for testing; restrict in production

# Optional: Enable SSH access for debugging (not recommended for production)
# enable_ssh_access = true
# ssh_access_cidrs = ["YOUR_IP/32"]

# Tags
tags = {
  Environment = "test"
  Project     = "eks-module-testing"
  ManagedBy   = "terraform"
}